I have all the verification I need. The current hero literally reads "What your backing is making possible on country" / "what we have come from" (the backward gratitude framing Ben wants killed), the gallery has three named-identifiable-face images with no consent flag in the data model, and the brief's "raise structure does not exist" claim is contradicted by `MATCH_TARGET`, `CAPITAL_STACK`, and `QBE_PROGRAM` already in the codebase.

Here is the adversarial review.

---

# Adversarial Review — Forward-Looking Dashboard Brief

## P0 — Must fix before any build (correctness, consent, the core reframe)

**P0-1. The brief's single biggest factual error: the `raise` structure already exists, so "Phase 2, build it from scratch" is wrong and will cause drift.**
The brief states (§2 Section 2, §6 Phase 2, §7 Q1): *"this does NOT exist in code today... QBE ~$400K lives only in kanban prose + memory... Smallest path: add a curated `raise` object."* That is false. Verified in the live tree:
- `src/lib/data/loi-pipeline.ts:100` exports `MATCH_TARGET = { cap: 400_000, note: '...up to $400K... must be at least matched by signed external commitments...' }`.
- `src/lib/data/funder-shared-content.ts:62` exports `CAPITAL_STACK` — a full forward stack with `{ layer, source, amount, status }` rows: Minderoo $1.5M "In conversation", SEFA $300K "In outreach", QBE "Up to $400K / Conditional on matched raise", $1M "Pipeline", Snow guarantee. Plus `QBE_PROGRAM` at line 70.
- `funder-pages.ts` already frames a `~$3M blended capital raise (target)`.

So Section 2 is **Phase 1, not Phase 2**: it consumes existing config, it does not invent a data system. Fix: rewrite Section 2's data note to **reuse `CAPITAL_STACK` + `MATCH_TARGET` + `QBE_PROGRAM`** and have `partner-dashboards.ts` reference them (not re-key the numbers — three copies of $400K is a reconciliation bug waiting to happen). The "single biggest content decision that blocks the reframe" (Q1) is largely already answered in code; the open question shrinks to "which committed figure is the public anchor," not "what is the structure."

**P0-2. Gratitude still leaks — and the brief's own Phase 0 keeps it live.** The brief claims "no gratitude framing" but Phase 0 says *"reuse the four existing panels as sections... No new data... shippable immediately."* The existing panels carry the exact backward copy Ben is killing. Verified current strings that survive Phase 0 untouched:
- `heroLine: 'What your backing is making possible on country'` (partner-dashboards.ts:74) — possessive "your backing," past-tense gift framing.
- `intro: '...where we have come from...'` (line 77) — explicit backward-looking.
- `contribution: { value: '~$493K', note: 'Anchor of the blended raise, fully received' }` (line 79) — a per-funder "this funder gave $X" tile, the precise thing to drop. The brief's Section 2 even proposes aggregating `contribution`/`funding[]` as "committed," which re-imports the donor-attribution frame through the back door.

Fix: Phase 0 must be a **layout-only** refactor explicitly flagged "copy still backward, do not ship to a funder." The reframe is not optional polish in Phase 1 — until the hero copy and the `contribution` tile are rewritten, the page still says thank-you. Add an acceptance test to Phase 1: grep the rendered page for `your backing`, `come from`, `fully received`, possessive funder framing → must be zero.

**P0-3. Consent violation is shipped, not flagged.** The brief correctly raises gallery consent as Open Question 3 — but treats it as a question for Ben while Phase 0/1 **ports the gallery as-is**. Verified the gallery contains identifiable faces with no consent field in the data model: `nic-with-elder-on-verandah.jpg` ("Nic with an Elder on a verandah"), `community-testing-bed-golden-hour.jpg` ("A family testing a bed"), `community-bed-assembly.jpg` ("Community members assembling a bed"). The `gallery` type is `{ src, alt }[]` — there is **no `consent` flag to gate on**. Per the brief's own non-extractive rule ("no identifiable photos without documented consent") and the CLAUDE.md consent rules, this is a P0 block, not an open question. Fix: (a) the new design ships with the three identifiable-face images **removed by default**, (b) extend the gallery type to `{ src, alt, consent: 'documented' | 'pending' | 'none' }` and render only `consent: 'documented'`, (c) consent-gating must be code (the brief says this for EL but not for the gallery — apply the same rule). An Elder on a verandah on a funder page without a recorded consent flag is exactly the extractive failure the whole brief claims to prevent.

**P0-4. "Committed aggregates from `funding[]`" risks re-publishing the mixed-ledger reconciliation problem.** The brief proposes Section 2's "committed" = sum of `getFundingSummary()` over `funding[]`. Per project memory, the Xero source (`Nicholas Marchesi 786af1ed`) is a mixed multi-project ledger and the $741,111 figure is the *settled, hand-reconciled* number — naive summation has repeatedly produced wrong totals (the entire Cluster 2 saga). Fix: Section 2's committed/secured anchor must be **`verifiedFinancials.revenueReceived` (741,111) only**, with a provenance citation, never a fresh client-side sum. State this as a hard constraint so the implementing agent doesn't "helpfully" recompute it.

## P1 — Should fix to be credibly world-class (the reframe quality, the data honesty, the nav)

**P1-1. Does it truly drop backward framing? Mostly yes in structure, but two design choices re-admit it.** Beyond the live-copy leaks (P0-2): (a) Section 4 "In service now" is well-placed *after* the forward story — good. (b) But the per-audience `contribution` tile and the "secured to date" anchor language still center "money received." World-class forward framing reframes even the proof: not "$493K received" but "the model is funded far enough to prove X; here is the gap to the next handover." Fix: kill the `contribution` tile entirely; fold any secured figure into Section 2's stack as "secured so far" inside the forward target, never as a standalone funder-credit.

**P1-2. The navigation is *good* sticky-nav, not yet exceptional — and the brief itself only claims "tab-grade orientation."** Scrollspy + progress bar + sticky headers is the competent baseline (Stripe docs, Linear). What's missing to be Pentagram-tier:
- **The rail should carry state, not just labels.** Each rail item should show its section's confidence posture (a tiny COUNTED/MODELLED/NOT-YET dot) so a funder skims the *epistemic shape* of the whole page from the rail. That turns a nav into an argument map. The brief puts confidence chips in sticky headers but not the rail — pull it forward.
- **One signature moment, executed flawlessly, beats five reveals.** The brief hedges with "sparse reveals" plus a scrollytelling pin plus a CSS progress bar plus view-timeline reveals. That's four animation systems. World-class is *restraint*: one pinned ownership-handover sequence (Section 3) done beautifully, everything else still. Cut the view-timeline reveals; they're decoration that reads as generic.
- **Print/PDF is named as a benefit but not designed.** Funders export. A `@media print` stylesheet that linearizes the page, expands the pin to its stacked form, and prints the confidence legend is a genuine differentiator the brief mentions ("one print target") but never specs. Add it.

**P1-3. Overclaim audit — places a number is implied but not available (you asked for every spot):**
- **"People reached = totalBeds × 2.5" and "plastic = deployed × 20kg"** are correctly tagged MODELLED in the brief — good, but the brief lets them sit as headline-row "answers" in the hero. A modelled multiplier in the hero, even chipped, reads as a count to a skimming funder. Fix: keep modelled figures *out* of the five hero answers; the hero is COUNTED-only or qualitative. Push modelled numbers to Section 4 with the assumption stated inline.
- **`get_fleet_kpis` RPC "if present"** (Section 4, hero). The brief says "wash cycles / machines-online from `get_fleet_kpis` RPC if present." "If present" is a tell — the implementing agent will render an empty/undefined metric. Verify the RPC exists before it's in the spec; if unverified, it must not appear as a planned hero/Section-4 number. (I did not find it confirmed; treat as NOT-YET until queried against Goods `cwsyhpiuepvdjtxaozwf`.)
- **`needBenchmarks` denominators ("X of an estimated Y")** — the brief is honest that these don't exist and defers to prose. Good. But Section 4's "community comparison" mandatory-trio slot will pressure an agent to invent a denominator. Make the trio's "community comparison" explicitly **prose-only until a cited ABS/AIHW figure exists**, with the citation required in-code.
- **`ownershipMilestones[].pctToOwnership`** — the brief says "do NOT invent an ownership-percent that isn't real," then puts `pctToOwnership` in the proposed type. Having the field invites the invention. Fix: drop `pctToOwnership` from the type entirely; use named stages only (`built | operating | community-run | community-owned`) until a real measure exists. A type field is a prompt to fill it.

**P1-4. Confidence-chip mapping has a silent-downgrade bug.** The brief maps `target` *and absent* → NOT-YET-MEASURED. Mapping *absent* to a grade means any metric that simply forgot to set `.confidence` renders as "NOT-YET-MEASURED" — which could *understate* a genuinely COUNTED asset number, or worse, an agent adds a metric and it silently shows the wrong grade. Fix: make `confidence` required (no default); a missing grade should fail the build (or render a loud "UNGRADED" dev-only marker), not silently pick a grade.

## P2 — Polish / de-risk

**P2-1. Single biggest BUILD-PLAN risk: Phase 0 ships the *structure* of the reframe with the *copy* of the old frame, and "shippable immediately" invites someone to push it to a funder.** This is the most dangerous line in the plan ("This alone delivers Ben's 'no tabs' and is shippable immediately"). It is shippable as an internal layout milestone only. Mitigation: gate Phase 0 behind the existing investors/partner cookie (already in `src/proxy.ts`) AND add the grep-acceptance-test from P0-2 to the Phase-1 definition-of-done so the backward copy physically cannot reach a funder URL. Name Phase 0 "layout spike, not funder-ready."

**P2-2. The multi-audience template is the right idea but under-tested against its own constraint.** The brief says the fixed scaffold (confidence legend + section order + per-section trio) "travels so a new partner cannot ship without them" — but the mechanism is config convention, not enforcement. A new `partner-dashboards.ts` object with `audience: 'supporter'` and no `confidence`/trio data will render blanks. Fix: make the `section.tsx` wrapper *require* the trio slots as non-optional props so omission is a TypeScript error, not a blank section. That's how the standard actually travels.

**P2-3. "Community holds the story. We hold the count." is a strong line — pressure-test it for accuracy.** It's elegant, but Goods *does* hold some story (the EL content it syndicates) and community holds authority over the count too (CARE: Authority to Control extends to data about them, not just narrative). The neat dichotomy slightly misrepresents the sovereignty model. Fix: have Ben/community sign off on the exact wording; consider "Community holds the authority. We hold the count, and show our working." Don't let a memorable line outrun the CARE principle it's invoking.

**P2-4. Cost-per-bed (Q2) — the brief defers, but the current `stewardshipPanel` shows "$534.79 / $684.79" and Phase 0 ports it.** Same trap as P0-2/P0-3: a deferred open question whose *current answer ships by default* in Phase 0. Decide before, not after: gate cost detail behind a "see the numbers" disclosure by default (safest), and don't let Phase 0 surface it on a forward page without a decision.

---

## The 3 highest-leverage moves from "good" to "world-class"

1. **Wire Section 2 to the live `CAPITAL_STACK` / `MATCH_TARGET` you already have, and make the rail an epistemic map.** This simultaneously kills the brief's biggest factual error (P0-1), makes the forward-reframe *real* (the #1 thing Ben asked for) instead of a deferred Phase-2 promise, and turns the nav from competent-sticky into a confidence-shaped argument the funder reads at a glance (P1-2). One change, three of the asks.

2. **Make non-extraction structurally impossible, not editorially requested.** Required `consent` field on gallery items (P0-3), required `confidence` grade on metrics (P1-4), required trio props on `section.tsx` (P2-2), and the grep-acceptance-test for backward copy (P0-2). The brief *describes* a non-extractive, confidence-graded standard; world-class is making the build *fail* when the standard isn't met. Convention is what the next agent ignores; the type system is what stops them.

3. **Cut to one signature interaction and design the print path.** Delete the view-timeline reveals and the redundant animation systems; invest all the craft in one flawless pinned ownership-handover sequence (the emotional core: the asset leaving your hands) plus a genuinely designed `@media print`/PDF for funders (P1-2). Restraint plus a deliverable export is the Stripe/Pentagram tell; five reveals is the template tell.

---

**Verdict:** Strong spine, correct instincts on confidence-grading and the forward reframe, and genuinely good on placing proof *after* direction. But it is not build-ready as written: it (a) misstates that the raise structure must be built when it already exists in three files, (b) ships backward gratitude copy and an unconsented Elder photo through its own Phase 0 "no new data, shippable" path, and (c) settles for competent sticky-nav while calling it best-in-class. Fix the four P0s and it becomes a brief you can hand to an implementing agent without it shipping something extractive or backward-framed.