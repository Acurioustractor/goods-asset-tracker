## What's next

**Distribute the allocated beds** to households — status converts `allocated → deployed` as institutions place each bed. Recipient names added as families come forward; `/admin/scans` Missed-installs alert catches each first scan automatically.

**Next batch** for ongoing fulfilment of the funder commitment (see [METRIC: commitment-progress-bar] above).

**Continue pulse-rate tracking** — each scan or pulse is a data point that this bed is still in use, still loved. Funder can verify ongoing impact via the public `/admin/scans` dashboard.

---
