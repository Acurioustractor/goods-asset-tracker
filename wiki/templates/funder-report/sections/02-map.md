## The trip on a map

[MAP: embed `/tmp/utopia-trip-full-map.html` — Leaflet, GPS clusters,
markers per cluster. Or screenshot the live `/admin/scans` map for static deck.]

Communities reached this period: **[METRIC: communities-served]**.

Every bed has a unique QR code tied to a public page at `goodsoncountry.com/bed/{id}`. Recipients can scan, see their bed named, leave a pulse signal, or message Goods directly.
