# Secrets and credentials
.env
.env.local
.env.production
*.pem
*.key
config.json
secure/
private/

# Dependencies
node_modules/
.pnp
.pnp.js

# Production builds
/build
/dist
/.next
out/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Operating System
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
.coverage
.pytest_cache/
htmlcov/

# Temporary files
*.tmp
*.temp
.cache/
tmp/
v2/tmp/

# Scratch / not for the repo (sensitive or session-local)
REAL grant docs/

# Generated video/render output — ~1.3GB, several files >100MB (GitHub rejects).
# Regenerable; keep source clips in storage, not git.
output/

# Heavy media assets — keep local / in object storage, not git (like output/).
# Originals, generated renders and deck-export artifacts, not app-served images.
generated-images/
design/_image-originals/
design/deck-assets/
design/brand/claude-design/
design/starred-images/

# Local browsing/staging folder for the System visuals review (symlinks into
# v2/public + generated-images/goods-illustrations/, built 2026-07-13). Not
# real files, but local-machine-only paths — regenerate with
# scripts/build-system-visuals-staging.sh rather than committing.
design/system-visuals/

# QR code generation (keep these in repo for now, but could move to CDN later)
# data/qr_codes/png/*.png
# data/qr_codes/svg/*.svg

# CLI config (user-specific)
.goods-tracker/

# Local Netlify folder
.netlify

# Claude/Cursor IDE files
.claude/

# Backup files
.env.local.backup*
*.backup-*

# GitHub workflows (if not wanted in this repo)
.github/workflows/

# Deployment configs
deploy/

# Global scripts symlink
scripts-global

# SQL scripts
supabase/*.sql

# Local tools and temp files
.obsidian/
.tldr/
.vercel/
autoresearch-macos/
media/
idiot_index_model.py
v2/check_requests.js
v2/test_queries.ts
v2/wash_report_week10_2026.json
.gstack/

# Grant attachment binaries — synced to Drive instead
wiki/raw/grants-archive/**/*.pdf
wiki/raw/grants-archive/**/*.docx
wiki/raw/grants-archive/**/*.doc
wiki/raw/grants-archive/**/*.xlsx
wiki/raw/grants-archive/**/*.xls

# Scratch screenshots from debugging sessions
/asset-register*.png
/bed-norman-machine.png
/edit-page-f25.png
/filter-applied.png
/filter-popover.png
/final-1920.png
/fleet-*.png
/retired-hidden.png
/telemetry-*.png
/user-viewport-*.png

# Tooling state directories
/.agents/
/.codex/
/.playwright-mcp/

# Scratch screenshots + source-media dumps at repo root (not repo content;
# real site assets live in v2/public/, source photos go to Empathy Ledger).
/*.png
/*.jpg
/*.jpeg
/*.JPG
/*.zip
/Utpoia Photos Export/
/Goods Beds */
/Story images/
/01.06.2026/

# EL photo-review local thumbnail cache (regenerable)
design/brand/kit/.el-cache/

# Canon kit — generated review surfaces (regenerable from v2/scripts; reference the gitignored cache)
design/brand/kit/el-photo-review.html
design/brand/kit/el-photo-index.json
design/brand/kit/canon-build.html
design/brand/kit/canon-build-index.json
design/brand/kit/photo-contact-sheet.html
design/brand/kit/*.pdf
design/brand/kit/*-preview.png

# Canon-baked artifact copies (regenerated by canon-render / render.sh)
*.resolved.html
# Canon-render EL pick download cache (regenerable)
design/.canon-cache/
# Rendered artifact outputs (regenerable via render.sh)
design/brand/claude-design/*.pdf
design/brand/claude-design/*-preview.png

# RED / sacred-source storyteller transcript extractions — never commit (Ben custodian; keep local only)
thoughts/shared/handoffs/goods-quote-analysis/
