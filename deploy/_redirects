# Netlify redirects
# IMPORTANT: Query parameter rules must come BEFORE the catch-all

# QR codes with asset_id parameter go to support form (index.html)
/*  asset_id=:id  /index.html?asset_id=:id  200!

# Root without params goes to public home page
/  /public-home.html  200!

# Direct support path
/support  /index.html  200
