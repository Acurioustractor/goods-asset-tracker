# Goods on Country — agent instructions

Read **`CLAUDE.md`** in this directory. It is the single set of instructions for every agent
working in this repo, not a Claude-specific file.

Working in `wiki/`? Its rules live in `wiki/AGENTS.md`.

This file used to carry its own copy of the repo instructions. It drifted out of sync and
started contradicting `CLAUDE.md` on the product description, the fonts and the database
rules, so the copy is gone. Add repo-wide guidance to `CLAUDE.md` only.
